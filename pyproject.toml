[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "veritas-audit"
version = "0.14.0"
description = "Evidence-first auditing for empirical social science research"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "cvxpy>=1.9,<2.0",
  "numpy>=1.26",
  "scipy>=1.11",
  "scs==3.2.11",
]

[project.optional-dependencies]
pdf = [
  "PyMuPDF==1.28.2",
  "pdfplumber==0.11.10",
]
attestation = [
  "cryptography>=46,<51",
]
web = [
  "fastapi>=0.116,<1",
  "python-multipart>=0.0.20,<1",
  "uvicorn>=0.35,<1",
]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.0",
  "ruff>=0.6",
]

[project.scripts]
veritas-harness = "veritas.harness.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/veritas"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 110
